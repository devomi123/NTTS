import { CheakNumDirective } from './cheak-num.directive';

describe('CheakNumDirective', () => {
  it('should create an instance', () => {
    const directive = new CheakNumDirective();
    expect(directive).toBeTruthy();
  });
});
